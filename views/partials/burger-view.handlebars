<li>
  {{id}}

  <button class="eat-burger" data-id="{{id}}" data-newsleep="{{devoured}}">
    {{#if devoured}}Eaten{{else}}Eat-Dat-Burger{{/if}}
  </button>

  <button class="btn btn-sucess delete-burger" data-id="{{this.id}}">Eat-Dat-Burger</button>
</li>